import { CustomforDirective } from './customfor.directive';

describe('CustomforDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomforDirective();
    expect(directive).toBeTruthy();
  });
});
